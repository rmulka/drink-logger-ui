import { BEER, WINE } from './drinkConstants';

export const BROWSE_BEERS_PATH = `/browse/${BEER}`;
export const BROWSE_WINES_PATH = `/browse/${WINE}`;
export const PUBLIC_PATH = '/';

export const BROWSE_PATH = '/browse/:resource';